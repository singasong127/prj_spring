<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.app.infra.member.MemberMapper">

	<resultMap id="resultMapObj"
		type="com.mycompany.app.infra.member.Member"></resultMap>

	<sql id="selectAll">
		seq
		, id
		, password
		, nickname
		, emailAccount
		, (SELECT
		cdName
		FROM code AS d
		WHERE d.cdSeq = m.emailDomain) AS emailDomain
		, (SELECT
		CASE
		WHEN gender = 4 THEN '남성'
		WHEN gender = 5 THEN '여성'
		ELSE '기타'
		END
		FROM code AS d
		WHERE d.cdSeq = m.gender) AS gender
		, age
		, agreeDT
		, delNy
	</sql>
	
	<sql id ="selectCommon">
   		FROM
		user AS m
		WHERE 1=1
		AND emailDomain = m.emailDomain
		AND gender = m.gender
		<if test="shId != null and !shId.equals('')">AND id LIKE CONCAT('%',#{shId},'%')</if>
		<if test="shNick != null and !shNick.equals('')">AND nickname LIKE CONCAT('%',#{shNick},'%')</if>
		<if test="shEmaccount != null and !shEmaccount.equals('')">AND emailAccount LIKE CONCAT('%',#{shEmaccount},'%')</if>
    </sql>
	
	<select id="selectOneCount" resultType="Integer">
		SELECT count(*)
		<include refid="selectCommon" />
	</select>
	
	<select id="selectList" resultMap="resultMapObj">
		SELECT 
			aa.* FROM (   	
	   		SELECT
				<include refid="selectAll"></include>
				<include refid="selectCommon"></include>
		) aa
		LIMIT #{rowNumToShow} OFFSET #{startRnumForMysql}
	</select>
	
	<select id="selectOne" resultMap="resultMapObj">
		SELECT
		<include refid="selectAll"></include>
		FROM user AS m
		WHERE 1=1
		AND seq = #{shSeq}
	</select>

	<select id="insert" resultMap="resultMapObj">
		INSERT INTO user(
		id
		, password
		, nickname
		, emailAccount
		, emailDomain
		, gender
		, age
		, agreeDT
		, delNy
		) values (
		#{shId}
		, #{shPwd}
		, #{shNick}
		, #{shEmaccount}
		, #{shEmain}
		, #{shGen}
		, #{shAge}
		, date_format(now(), '%Y%m%d%H%i%s')
		, 0
		)
	</select>

</mapper>    
    